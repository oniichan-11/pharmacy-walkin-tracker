# pharmacy-walkin-tracker
A Python-native Streamlit app for **Pharmacy Direct** to log the items walk-in customers ask for that you *can't* sell — across both branches — and turn that into live **unmet-demand** metrics for restocking and range decisions. This app captures it in a few seconds at the counter.
